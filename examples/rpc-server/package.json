{
    "name": "rust-rpc-server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
      "deploy": "cargo install worker-build ; wrangler deploy",
      "dev": "wrangler dev"
    },
    "devDependencies": {
      "wrangler": "^4.20.0"
    }
  }
